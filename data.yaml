---
# githubAPIkey: myAPIkey
httpTimeout: 3
httpInsecure: false
saveLocation: /tmp/tkn-210102

bins:
  - cli: tkn
    owner: tektoncd
    repo: cli
    match: Linux_x86_64
    # baseURL: github.com
    # download: true
